# File: nginx/default.erb

server {
    listen 80 default_server;
    listen [::]:80 default_server;

    server_name _;

    location / {
        root /var/www/html;
        index index.html;
    }

    add_header X-Served-By <%= @server_hostname %>;
}

